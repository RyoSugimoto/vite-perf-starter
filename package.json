{
  "name": "vite-perf-starter",
  "version": "1.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "report": "ts-node tools/bundle-report.ts",
    "perf:check": "lhci autorun",
    "deploy:cf": "wrangler pages deploy dist --project-name=vite-perf-starter",
    "deploy:netlify": "netlify deploy --prod --dir=dist"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^6.0.0",
    "@lhci/cli": "^0.12.0",
    "rollup-plugin-visualizer": "^5.9.0",
    "ts-node": "^10.9.2",
    "wrangler": "^3.80.0",
    "netlify-cli": "^17.0.0"
  }
}
